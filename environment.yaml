name: pubupdate
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - aiohttp
      - bibtexparser
      - doi2bib
      - tomli
      - tomli-w
      - tqdm
      - yake
      - pandas
      - openpyxl
      - aiolimiter